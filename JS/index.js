/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
    let max=0; 
    const map={}; 
    for(let i=0;i<prices.length;i++)
        map.prices[i]= i;
    
};
console.log(maxProfit([7,1,5,3,6,4])); 